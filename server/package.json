{
  "name": "stredio-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Backend for STREDIO — TMDB catalog/search proxy + addon install-by-URL engine",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
